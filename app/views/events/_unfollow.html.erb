<div class="row">
  <div class="col-md-6 col-md-offset-3">

<%= form_for(current_user.active_relationships.find_by(followed_id: @event.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "取り消し", class: "btn" %>
<% end %>

  </div>
</div>