<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav>
      <ul>
          <% if logged_in? %>
            <div class=name><%= link_to "ようこそ　#{current_user.name} さん"%></div>
          <% end %>
          <div class="menu"><%= link_to "Home",  root_path %></div>
          <div class="menu"><%= link_to "My page",  current_user %></div>
          <% if logged_in? %>
            <div class="menu"><%= link_to "Log out", logout_path, method: :delete %></div>
          <% end %>
      </ul>
    </nav>
  </div>
</header>
